spring.application.name=GitMazonMasterNode
server.port=8082
## JPA
spring.datasource.url=jdbc:mysql://localhost:3306/gitmazon?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=guo
spring.datasource.password=guo12345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
cors.allowed.origins=http://localhost:8080
## Github OAuth
spring.security.oauth2.client.registration.github.client-id=${github.client-id:your-client-id}
spring.security.oauth2.client.registration.github.client-secret=${github.client-secret:your-client-secret}
spring.security.oauth2.client.registration.github.redirect-uri=${github.redirect-uri:your-redirect-uri}
spring.security.oauth2.client.provider.github.authorization-uri=${github.authorization-uri:your-authorization-uri}
spring.security.oauth2.client.provider.github.token-uri=${github.token-uri}
spring.security.oauth2.client.provider.github.user-info-uri=${github.user-info-uri:your-user-info-uri}
spring.security.oauth2.client.registration.github.scope=${github.scope:your-github-token}
## jwt
jwt.secret=${jwt.secret.env:fGaSapcMsjJ8sHOD9kikjCxWOLezjVL7}
## securityGroupId
security.group.id=${security.group.id.env:your-aws-security-group-id}
## ip
worker.nodes=${worker.nodes.env:worker-nodes}
imageProducer.host=${imageProducer.host.env:imageProducer-host}
nginx.host=${nginx.host.env:nginx-host}